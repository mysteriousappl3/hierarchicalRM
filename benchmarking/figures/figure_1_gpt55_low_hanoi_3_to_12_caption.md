Figure 1. GPT 5.5 low-reasoning Hanoi success from 3 to 12 disks. Each point is a single benchmark run scored as 100% if the run was legal and reached the final goal state, otherwise 0%. The no-framework baseline uses one direct MoveHoop plan, inner-outer adds LLM verifier/replanning without H1/H2, and the full framework adds inner-outer plus H1/H2 hierarchy. These points are not 25-sample aggregate accuracies.
