(define (domain mytrajectoryconstraintsremover_blocksworld_problem-domain)
 (:requirements :strips :typing :negative-preconditions :disjunctive-preconditions :equality :conditional-effects :existential-preconditions :action-costs)
 (:types block)
 (:predicates (clear ?obj - block) (ontable ?obj - block) (handempty) (holding ?obj - block) (on ?obj1 - block ?obj2 - block))
 (:functions (total-cost))
 (:action pickup
  :parameters ( ?obj - block)
  :precondition (and (clear ?obj) (ontable ?obj) (handempty) (exists (?x - block)
 (and (clear ?x) (not (= ?obj ?x)))))
  :effect (and (holding ?obj) (not (clear ?obj)) (not (ontable ?obj)) (not (handempty)) (increase (total-cost) 1)))
 (:action pickup_double
  :parameters ( ?obj - block)
  :precondition (and (ontable ?obj) (handempty) (exists (?topobj - block)
 (and (on ?topobj ?obj) (clear ?topobj))) (exists (?x - block)
 (and (clear ?x) (not (on ?x ?obj)))))
  :effect (and (holding ?obj) (not (ontable ?obj)) (not (handempty))(forall (?topobj - block) (when (on ?topobj ?obj) (not (clear ?topobj)))) (increase (total-cost) 1)))
 (:action putdown
  :parameters ( ?obj - block)
  :precondition (and (holding ?obj) (exists (?x - block)
 (or (and (not (exists (?topobj - block)
 (on ?topobj ?obj))) (= ?obj ?x)) (on ?x ?obj) (clear ?x))))
  :effect (and (handempty) (ontable ?obj) (not (holding ?obj)) (when (not (exists (?topobj - block)
 (on ?topobj ?obj))) (clear ?obj))(forall (?topobj - block) (when (on ?topobj ?obj) (clear ?topobj))) (increase (total-cost) 1)))
 (:action stack
  :parameters ( ?obj - block ?underobj - block)
  :precondition (and (clear ?underobj) (holding ?obj) (exists (?x - block)
 (or (and (not (exists (?topobj - block)
 (on ?topobj ?obj))) (= ?obj ?x)) (on ?x ?obj) (and (clear ?x) (not (= ?underobj ?x))))))
  :effect (and (handempty) (on ?obj ?underobj) (not (clear ?underobj)) (not (holding ?obj)) (when (not (exists (?topobj - block)
 (on ?topobj ?obj))) (clear ?obj))(forall (?topobj - block) (when (on ?topobj ?obj) (clear ?topobj))) (increase (total-cost) 1)))
 (:action unstack
  :parameters ( ?obj - block ?underobj - block)
  :precondition (and (on ?obj ?underobj) (clear ?obj) (handempty) (exists (?x - block)
 (or (= ?underobj ?x) (and (clear ?x) (not (= ?obj ?x))))))
  :effect (and (holding ?obj) (clear ?underobj) (not (on ?obj ?underobj)) (not (clear ?obj)) (not (handempty)) (increase (total-cost) 1)))
 (:action unstack_double
  :parameters ( ?obj - block ?underobj - block)
  :precondition (and (on ?obj ?underobj) (handempty) (exists (?topobj - block)
 (and (on ?topobj ?obj) (clear ?topobj))) (exists (?x - block)
 (or (= ?underobj ?x) (and (clear ?x) (not (on ?x ?obj))))))
  :effect (and (holding ?obj) (clear ?underobj) (not (on ?obj ?underobj)) (not (handempty))(forall (?topobj - block) (when (on ?topobj ?obj) (not (clear ?topobj)))) (increase (total-cost) 1)))
)
